# سير عمل إضافة درس — Lesson Workflow

> هذا الملف يوثّق **العملية الملزمة** لإضافة أي درس إلى المنصة.
> الهدف: إضافة دروس المنهاج واحدة واحدة، بلا إعادة بناء المنصة،
> وبلا إخلال بالكتاب المدرسي مصدرًا مرجعيًا.

---

## المبادئ غير القابلة للتفاوض

| # | المبدأ |
|---|---|
| 1 | **كتاب الرياضيات هو المصدر المرجعي** للمحتوى والترتيب. |
| 2 | الكتاب ليس سقف الشرح: نشرح أعمق وأوضح، مع **الحفاظ الكامل** على كل عنصر من الكتاب. |
| 3 | كل عنصر يميّز بين **من الكتاب** (`provenance: "book"` + `bookRef`) و**إضافة تعليمية** (`"extra"` + `reason`). |
| 4 | كل درس له **سجل تغطية** `coverage.json` و**تدقيق آلي** يثبت التغطية. |
| 5 | أشكال الكتاب **محتوى منهجي**: تُعاد بناؤها بإحداثيات دقيقة، لا تقريبًا زخرفيًا. |
| 6 | أسئلة الطالب لا تحمل حلولها في DOM العادي. |
| 7 | حلول المعلم مفصولة: لا تدخل حزمة المتصفح (خادم فقط). |
| 8 | العربية RTL، والرياضيات LTR معزولة دائمًا. |
| 9 | الكسور تُرسم **مكدّسة** (بسط فوق مقام). |
| 10 | معرّفات الدروس وترتيبها لا تتغيّر. |
| 11 | لا يُخترع محتوى منهجي، ولا تُحذف دروس، ولا يُعاد ترقيمها. |
| 12 | لا تُعاد صياغة نص الكتاب أو يُختصر إلا بطلب صريح. |
| 13 | البوابات الآلية **حدّ أدنى** للجودة، لا إثبات لها: الجودة التربوية تُراجع يدويًا. |

---

## الخطوات

### 1) استلام المصدر

يُستلم من صاحب المنصة: صفحات الكتاب (نصًا) للدرس، بترتيبها.

### 2) إنشاء مجلد الدرس

```text
src/lessons/<lesson-id>/
  content.ts        ← المحتوى (نموذج المحتوى v1 من src/content/types.ts)
  coverage.json     ← سجل التغطية (قالب: docs/coverage-ledger-template.md)
  figures.ts        ← (اختياري) أشكال الدرس إن كانت كثيرة
```

`<lesson-id>` **يجب** أن يطابق معرّف الدرس الموجود في `src/data/curriculum.ts`
حرفيًا (مثل `algebra-u1-l1`). التدقيق يرفض أي معرّف غير موجود في المنهاج.

### 3) كتابة المحتوى

اجعل كل خطوة (Step) شاشة مستقلة: 20–45 خطوة للدرس الواحد عادة.

- ابدأ بـ `cover` ثم `objectives` (أهداف الكتاب كما وردت).
- لكل مفهوم: خطوة `lesson` بكتل شرح.
- كل مثال من الكتاب: كتلة `example` بـ `provenance: "book"` و`bookRef` دقيق.
- كل شرح إضافي/مثال إضافي: `provenance: "extra"` مع `reason` يوضّح سبب الإضافة.
- لكل حلّ: `worked` بخطوات **كل خطوة فيها `why` إلزامي**.
- الأخطاء الشائعة الحقيقية فقط: `misconception`.
- الأشكال: `figure` ببيانات إحداثية (`FigureSpec`).
- التفاعلات: `activity` أو خطوة `interactive`/`practice` حسب الطول المطلوب.
- اختم بـ `summary` ثم `closing`، واختبار `quiz` عند الحاجة.

### 4) تسجيل الدرس

سطر واحد في `src/lessons/registry.ts`:

```ts
const LESSONS: Record<string, () => Promise<{ default: LessonModule }>> = {
  "<lesson-id>": () => import("./<lesson-id>/content"),
};
```

ثم في `src/data/curriculum.ts` غيّر حالة الدرس إلى `"available"` فقط
(لا تغيّر المعرّف ولا الترتيب ولا الرقم).

### 5) سجل التغطية

املأ `coverage.json`: لكل قسم من المصدر، نصّه الكامل ورقم الخطوة التي تعرضه.
الأعداد المعلنة فيه تُقارَن آليًا بالمحتوى.

### 6) التدقيق (ثلاث طبقات)

```bash
npm run audit:lesson -- <lesson-id>   # طبقة واحدة: هذا الدرس
npm run audit:fixture                 # نموذج تقني: يتحقق من خط التدقيق نفسه
npm run verify                        # كل البوابات + البناء
```

الطبقات:

| الطبقة | ماذا تفحص |
|---|---|
| **المصدر والأعداد** | كل نص كتابي موجود كاملًا، والأعداد المعلنة مطابقة، وكل عنصر له `provenance` سليم |
| **المحتوى** | تعليل لكل خطوة، لا كسور مسطّحة، لا أوامر LaTeX مجهولة، هندسة الأشكال متماسكة |
| **العرض الفعلي** | تُصيَّر كل خطوة بـ React ويُفحص DOM الطالب: لا LaTeX خام، الرياضيات معزولة LTR، الكسور مكدّسة، الأشكال SVG سليمة، ولا حلّ ولا إجابة في DOM قبل تصرّف الطالب |

الطبقة الثالثة هي الأهم لأنها تفحص **ما يصل فعلًا إلى شاشة الطالب**، لا ما نعتقد أننا كتبناه.

أي فشل = خلل يجب إصلاحه، لا تجاوزه.

### 7) المراجعة التربوية (يدوية)

اقرأ الدرس كطالب: هل يفهم المفهوم من الشرح وحده؟ (انظر `docs/math-lesson-standard.md`).

---

## قواعد المحتوى الملزمة

1. **لا محتوى مخترع يُنسب للكتاب.** الإضافات تُوسم دائمًا `"extra"`.
2. **لا حذف ولا اختصار** لنص الكتاب؛ التدقيق يقارن النصوص.
3. **الترتيب هو ترتيب الكتاب**؛ الإضافات تُدرج حول الفكرة لا بدلًا منها.
4. **الرياضيات في النص تُكتب بين `$...$`** وإلا ظهرت أوامر LaTeX خامًا (والتدقيق يرفضها).
5. **الكسور `\frac{...}{...}` فقط** — لا `1/2` أبدًا.
6. **الأشكال إحداثية** — لا مواضع تقريبية للنقاط والعلامات.
7. **لا كلمات مرور في الكود**، ولا إجابات في أي ملف يُحزَّم مع المتصفح.
8. **لا `Math.random`** في العرض (الترتيب يجب أن يكون ثابتًا بين الخادم والمتصفح).
9. **المحتوى المحجوب لا يُركَّب في DOM:** كل ما لا يجوز أن يراه الطالب قبل تصرّفه
   يُوضع داخل عنصر يحمل `data-locked="true"`، ولا يُركَّب إلا بعد التصرّف.
   التدقيق الآلي يرفض وجود هذه العلامة في HTML الأولي — فالإخفاء بـ CSS مرفوض بنيويًا.

---

## ما لا يفعله هذا السير

- لا يُنشئ محتوى بالنيابة عن صاحب المنصة.
- لا يُعيد صياغة نصوص الكتاب.
- لا يُغيّر معرّفات أو ترتيب الدروس الحالية.
- لا يعتبر نجاح التدقيق شهادة جودة تربوية.
