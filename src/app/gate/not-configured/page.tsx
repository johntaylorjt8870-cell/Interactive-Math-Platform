import type { Metadata } from "next";
import GateNotice from "@/components/gate/GateNotice";

// ============================================================
// خطأ إعداد البوابة — /gate/not-configured
// ============================================================
// يعيد proxy كتابة كل صفحة محمية إلى هنا بحالة **503** عندما تكون
// البيئة إنتاجية وكلمة مرور الموقع غير مضبوطة — أي أن الموقع «مقفل
// لسبب إعدادي» لا «مفتوح احتياطيًا».
//
// زيارة المسار مباشرةً تُعيد 200 (لأنها صفحة معلومات لا مورد محمي)،
// أمّا القفل الفعلي فيحدث في proxy وهو ما يهمّ الفحوص والمراقبة.
// ============================================================

export const metadata: Metadata = {
  title: "الموقع مقفل: إعداد ناقص",
  robots: { index: false, follow: false },
};

export default function GateNotConfiguredPage() {
  return <GateNotice />;
}
